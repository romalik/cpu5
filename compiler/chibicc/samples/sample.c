// sample.c — simple showcase of C syntax for AST dumping
/*
int square(int x) {
    return x * x;
}

int max(int a, int b) {
    if (a > b)
        return a;
    else
        return b;
}

int sum_array(int *arr, int n) {
    int total = 0;
    for (int i = 0; i < n; i = i + 1)
        total = total + arr[i];
    return total;
}

int main(void) {
    int nums[3] = {1, 2, 3};
    int s = sum_array(nums, 3);
    int m = max(s, square(5));

    return m;
}


*/

int main() {
    char a = 1;
    char b = a + 4;
    
}